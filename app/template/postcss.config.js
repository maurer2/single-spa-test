const postcssNormalize = require('postcss-normalize');

module.exports = {
  plugins: [
    postcssNormalize,
    //postcssNormalize,
    // 'postcss-import': {},
    // 'postcss-preset-env': {},
    // 'cssnano': {}
  ],
}
